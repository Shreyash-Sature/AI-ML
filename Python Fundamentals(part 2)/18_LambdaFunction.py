"""lambda function :
it does not need a name.
It is mainly used when you need a short function for a short time.
    
lambda arguments/parameters(a,b,c): expression

lambda → keyword
arguments → input values (like function parameters)
expression → the result returned (must be a single expression)"""

 
sum = lambda a,b : a+b
print(sum(4,5))

